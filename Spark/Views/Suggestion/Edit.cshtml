@model Spark.Models.Suggestion

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Suggestion</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.SparkID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SparkID)
            @Html.ValidationMessageFor(model => model.SparkID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ParentSuggestionID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ParentSuggestionID)
            @Html.ValidationMessageFor(model => model.ParentSuggestionID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UpvoteCount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.UpvoteCount)
            @Html.ValidationMessageFor(model => model.UpvoteCount)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Active)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Active)
            @Html.ValidationMessageFor(model => model.Active)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
